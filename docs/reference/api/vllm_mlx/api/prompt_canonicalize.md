<!-- Generated by scripts/gen_api_reference.py. Do not edit directly. -->

# `vllm_mlx.api.prompt_canonicalize`

System-prompt canonicalization helpers.

[View the complete module source at #L1-L51](https://github.com/waybarrios/vllm-mlx/blob/gh-pages/vllm_mlx/api/prompt_canonicalize.py#L1-L51).

## API details

Each callable below includes its exact signature, type annotations, inputs, defaults, return contract, documented exceptions, implementation source, and parsed docstring sections when the source provides them.

::: vllm_mlx.api.prompt_canonicalize
    options:
      members:
        - _STRIPPERS
        - canonicalize_system_prompt
        - canonicalize_system_messages
      filters: []
      show_if_no_docstring: true

## Complete contract reference

Expand any definition for its exact inputs, annotations, defaults, return contract, directly raised exceptions, source-grounded behavior, and immutable line link. This section includes private and nested definitions that ordinary API generators omit.

<details class="api-contract" id="contract-vllm_mlx.api.prompt_canonicalize.canonicalize_system_prompt" markdown="1">
<summary><code>vllm_mlx.api.prompt_canonicalize.canonicalize_system_prompt</code> · function</summary>

```python
vllm_mlx.api.prompt_canonicalize.canonicalize_system_prompt(text: str | None) -> str | None
```

Remove known non-semantic volatile lines from system prompt text.

**Parameters**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `text` | `str \| None` | `yes` | `none` | Required positional or keyword input. |

**Returns**

- Type: `str | None`
- Direct return expressions: `None`; `text`

**Exceptions and behavior**

Function `canonicalize_system_prompt` calls `pattern.sub`; has 2 explicit return paths.
No direct `raise` statement appears in this definition.

[View source #L17-L24](https://github.com/waybarrios/vllm-mlx/blob/gh-pages/vllm_mlx/api/prompt_canonicalize.py#L17-L24).

</details>

<details class="api-contract" id="contract-vllm_mlx.api.prompt_canonicalize.canonicalize_system_messages" markdown="1">
<summary><code>vllm_mlx.api.prompt_canonicalize.canonicalize_system_messages</code> · function</summary>

```python
vllm_mlx.api.prompt_canonicalize.canonicalize_system_messages(messages: list[dict]) -> list[dict]
```

Canonicalize string content on system-role messages without mutation.

**Parameters**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `messages` | `list[dict]` | `yes` | `none` | Required positional or keyword input. |

**Returns**

- Type: `list[dict]`
- Direct return expressions: `canonicalized if changed else messages`

**Exceptions and behavior**

Function `canonicalize_system_messages` calls `message.get`, `canonicalized.append`, `isinstance`, `canonicalize_system_prompt`; returns `canonicalized if changed else messages`.
No direct `raise` statement appears in this definition.

[View source #L27-L51](https://github.com/waybarrios/vllm-mlx/blob/gh-pages/vllm_mlx/api/prompt_canonicalize.py#L27-L51).

</details>

## Complete symbol map

This map also includes private definitions and nested helpers. The signature column exposes every explicit input even when an internal helper has no dedicated parameter prose.

| Symbol | Kind | Signature and inputs | What it does | Source |
| --- | --- | --- | --- | --- |
| [`canonicalize_system_prompt`](#contract-vllm_mlx.api.prompt_canonicalize.canonicalize_system_prompt) | function | `canonicalize_system_prompt(text: str \| None) -> str \| None` | Remove known non-semantic volatile lines from system prompt text. | [#L17-L24](https://github.com/waybarrios/vllm-mlx/blob/gh-pages/vllm_mlx/api/prompt_canonicalize.py#L17-L24) |
| [`canonicalize_system_messages`](#contract-vllm_mlx.api.prompt_canonicalize.canonicalize_system_messages) | function | `canonicalize_system_messages(messages: list[dict]) -> list[dict]` | Canonicalize string content on system-role messages without mutation. | [#L27-L51](https://github.com/waybarrios/vllm-mlx/blob/gh-pages/vllm_mlx/api/prompt_canonicalize.py#L27-L51) |
